spring:
    datasource:
        url: jdbc:mariadb://myinstance.cciqlc2li0ke.ap-northeast-2.rds.amazonaws.com/myinstance
        driverClassName: org.mariadb.jdbc.Driver
        username: myinstance
        password: myinstance

    jpa:
        database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
        hibernate:
            ddl-auto: create
        # show-sql: true
        # properties:
        #    hibernate:
        #        format_sql: true
